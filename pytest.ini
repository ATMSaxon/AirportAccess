[pytest]
testpaths = tests
addopts = -q --maxfail=3 --strict-markers
markers =
    slow: marks tests that take more than a few seconds
    integration: end-to-end tests that touch real files
    network: hits real network (skipped by default)
